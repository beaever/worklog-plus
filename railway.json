{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @worklog-plus/backend exec prisma generate && pnpm --filter @worklog-plus/backend build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @worklog-plus/backend start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
